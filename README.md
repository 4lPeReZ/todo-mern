# todo-mern
This GitHub project is a To-Do List app built with the MERN stack 

1. Configure the environment variable to connect to the MongoDB database.
2. Deploy the backend as well.
3. Update the backend URL in the constants.js file with the deployed URL.
4. Perform the build using `npm run build`.
5. Upload the `build` folder to the deployment location for the frontend.